enum TaskStatus { pending, submitted, approved, rejected }
enum DonationStatus { receiptGenerated, receiptPending }
enum RequestStatus { pending, approved, rejected }
enum MouStatus { pending, approved, rejected }
enum MeetingStatus { scheduled, completed }
